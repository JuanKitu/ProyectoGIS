import { VacioDescripcionPipe } from './vacio-descripcion.pipe';

describe('VacioDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new VacioDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
